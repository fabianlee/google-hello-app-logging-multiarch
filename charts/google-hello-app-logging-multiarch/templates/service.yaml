apiVersion: v1
kind: Service
metadata:
  name: {{ include "google-hello-app-logging-multiarch.fullname" . }}
  labels:
    {{- include "google-hello-app-logging-multiarch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "google-hello-app-logging-multiarch.selectorLabels" . | nindent 4 }}
