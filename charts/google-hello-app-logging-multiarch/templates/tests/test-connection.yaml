apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google-hello-app-logging-multiarch.fullname" . }}-test-connection"
  labels:
    {{- include "google-hello-app-logging-multiarch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google-hello-app-logging-multiarch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
